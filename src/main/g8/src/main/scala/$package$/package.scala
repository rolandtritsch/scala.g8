package org.tritsch

/** [[$package$]] object.
  *
  * This package implements a [[Hello HelloWorld]] object (and all tests for it).
  */

package object example {
  /** The prefix that is used in the implementation of the [[Hello.greeting]].
    */
  val prefix = "hello"
}
